<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="OverItProject.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <StackLayout x:Name="mainPageView">
        <SearchBar Placeholder="Cerca una foto" TextChanged="SearchBar_TextChanged" />
        <CollectionView ItemsSource="{Binding ListPhotos}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="0"
                    Orientation="Vertical"
                    Span="3"
                    VerticalItemSpacing="0" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding ImageUrl}">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Path=BindingContext.ImageTapped, Source={x:Reference mainPageView}}" CommandParameter="{Binding .}" />

                        </Image.GestureRecognizers>
                    </Image>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>

</ContentPage>
